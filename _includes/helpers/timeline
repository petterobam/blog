

<div class="archive" >
{% for post in site.posts  %}
    {% if post.layout == 'post' %}
        {% capture this_year %}{{ post.date | date: "%Y" }}{% endcapture %}
        {% capture this_month %}{{ post.date | date: "%m" }}{% endcapture %}
        {% capture next_year %}{{ post.previous.date | date: "%Y" }}{% endcapture %}
        {% capture next_month %}{{ post.previous.date | date: "%m" }}{% endcapture %}

        {% assign post_ids = post.id | split: "/" %}
        {% if post_ids.last != 'life' %}
            {% continue %}
        {% endif %}  

        {% if forloop.first %}
            <h3 id="{{ this_year }}-ref">{{this_year}}</h3>
        {% else %}
            {% if this_year != next_year %}
                <h3 id="{{ next_year }}-ref">{{next_year}}</h3>
            {% endif %}  
        {% endif %}  
        
        <section>
            <span class="point-time "></span>
            <time datetime='{{ post.date | date: "%m" }}'>
                <span>
                    <h3 id="{{ this_year }}-{{ this_month }}-ref">{{ this_month }}-{{ post.date | date: "%d" }}</h3>
                </span>
            </time>
            <aside>
                <a href="{{ post.url }}" title="{{ post.title }}">{{ post.title }}</a>
                <span><font color="gray">{{ post.date | date: "%Y" }}-{{ post.date | date: "%m" }}-{{ post.date | date: "%d" }}</font></span>
            </aside>
        </section>    
        <section>
            <aside>
                <article class="unit-article layout-post">
                    <div class="unit-inner unit-article-inner">
                        <div itemscope itemtype="http://schema.org/Article" class="content">
                            <div class="bd article-content">
                                <div class="entry-content">
                                    <div id="articleBody" class="life-news-body" itemprop="articleBody">
                                        {{ post.content }}
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </article>
            </aside>
        </section>        
    {% endif %}
{% endfor %}
</div>